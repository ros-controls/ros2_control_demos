passthrough_controller:
  interfaces: {
    type: string_array,
    default_value: [],
    description: "Names of the interfaces to be forwarded",
    validation: {
          not_empty<>: null,
          unique<>: null,
        }
  }
